% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_airnow_forecast_history.R
\name{get_airnow_forecast_history}
\alias{get_airnow_forecast_history}
\title{Get past air quality forecasts}
\usage{
get_airnow_forecast_history(
  area,
  start_date,
  end_date,
  range = NULL,
  parameter = NULL,
  clean_names = TRUE,
  api_key = get_airnow_key()
)
}
\arguments{
\item{area}{Reporting area code such as \code{"ca064"} (required). See
\link{airnow_areas} or \code{\link[=get_airnow_reporting_area]{get_airnow_reporting_area()}}.}

\item{start_date, end_date}{First and last \emph{valid} date to include, as
\code{Date} objects or \code{"YYYY-MM-DD"} strings.}

\item{range}{Optional forecast lead time in days to keep (a positive
whole number). \code{NULL} (default) keeps every lead time.}

\item{parameter}{Optional pollutant to keep: one of \code{"ozone"},
\code{"pm2.5"}, \code{"pm10"}, \code{"co"}, \code{"no2"}, \code{"so2"}.}

\item{clean_names}{Whether column names should be converted to snake_case
(default: \code{TRUE}). With \code{FALSE}, the API's lowerCamelCase names are kept.}

\item{api_key}{AirNow API key}
}
\value{
A tibble with the same columns as \code{\link[=get_airnow_forecasts]{get_airnow_forecasts()}}.
}
\description{
\code{get_airnow_forecast_history()} retrieves forecasts that were \emph{valid} on
dates in the given range for one reporting area. Every forecast lead time
is returned unless \code{range} narrows it: \code{range = 1} keeps only the
forecast issued the day before each valid date.
}
\section{Requests made}{

One request per call. Every request counts against AirNow's limit of
500 requests per hour per key.
}

\examples{
\dontrun{
get_airnow_forecast_history("md008", "2026-01-13", "2026-01-14")
get_airnow_forecast_history("md008", "2026-01-13", "2026-01-13",
  range = 1, parameter = "pm2.5"
)
}
}
