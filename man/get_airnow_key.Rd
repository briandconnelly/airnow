% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/credentials.R
\name{get_airnow_key}
\alias{get_airnow_key}
\alias{set_airnow_key}
\title{Manage your AirNow API key}
\usage{
get_airnow_key(ask = is_interactive())

set_airnow_key(key = NULL, ask = is_interactive())
}
\arguments{
\item{ask}{Whether to prompt for the key if none is set. Prompting only
works in interactive sessions.}

\item{key}{The API key to use. If \code{NULL} and \code{ask} is \code{TRUE}, you are
prompted for one.}
}
\value{
\code{get_airnow_key()} returns a string.

\code{set_airnow_key()} returns the key, invisibly.
}
\description{
\code{get_airnow_key()} returns the configured AirNow API key. If
no key is set and \code{ask} is \code{TRUE} in an interactive session, you are
prompted for one; otherwise an error is raised.

The key is read from the \code{AIRNOW_API_KEY} environment variable. To set it
permanently, add \code{AIRNOW_API_KEY=your-key} to your \verb{~/.Renviron} file.
Keys are issued at \url{https://docs.airnowapi.org/account/request/}.

\code{set_airnow_key()} sets the AirNow API key for the current
session.
}
\examples{
\dontrun{
get_airnow_key()
}
\dontrun{
set_airnow_key(key = "4d36e978-e325-11ce-bfc1-08002be10318")
}
}
