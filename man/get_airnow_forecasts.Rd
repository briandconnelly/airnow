% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_airnow_forecasts.R
\name{get_airnow_forecasts}
\alias{get_airnow_forecasts}
\title{Get current air quality forecasts}
\usage{
get_airnow_forecasts(
  zip = NULL,
  latitude = NULL,
  longitude = NULL,
  area = NULL,
  clean_names = TRUE,
  api_key = get_airnow_key()
)
}
\arguments{
\item{zip}{ZIP code, a 5-digit numeric string (e.g., \code{"90210"})}

\item{latitude}{Latitude in decimal degrees}

\item{longitude}{Longitude in decimal degrees}

\item{area}{Reporting area code such as \code{"ca064"}. See \link{airnow_areas} or
\code{\link[=get_airnow_reporting_area]{get_airnow_reporting_area()}}. When given, \code{zip}, \code{latitude}, and
\code{longitude} are ignored.}

\item{clean_names}{Whether column names should be converted to snake_case
(default: \code{TRUE}). With \code{FALSE}, the API's lowerCamelCase names are kept.}

\item{api_key}{AirNow API key}
}
\value{
A tibble with one row per pollutant per forecast day. \code{parameter}
is a factor with levels \code{ozone}, \code{pm2.5}, \code{pm10}, \code{co}, \code{no2}, \code{so2};
\code{category_name} is an ordered factor from Good to Hazardous. \code{latitude}
and \code{longitude} describe the reporting area and come from \link{airnow_areas}.
An \code{aqi} of \code{-1} means the agency issued a categorical forecast only.
}
\description{
\code{get_airnow_forecasts()} retrieves the forecasts currently issued for a
reporting area, located by ZIP code, latitude/longitude, or reporting
area code. One row is returned per pollutant per forecast day. For
forecasts valid on past dates, use \code{\link[=get_airnow_forecast_history]{get_airnow_forecast_history()}}.
}
\section{Requests made}{

One request per call. Every request counts against AirNow's limit of
500 requests per hour per key.
}

\examples{
\dontrun{
get_airnow_forecasts(zip = "90210")
get_airnow_forecasts(area = "ca064")
}
}
\seealso{
\code{\link[=get_airnow_forecast_history]{get_airnow_forecast_history()}} for past forecasts.
}
