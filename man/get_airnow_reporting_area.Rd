% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_airnow_reporting_area.R
\name{get_airnow_reporting_area}
\alias{get_airnow_reporting_area}
\title{Find the AirNow reporting area for a location}
\usage{
get_airnow_reporting_area(
  zip = NULL,
  latitude = NULL,
  longitude = NULL,
  api_key = get_airnow_key()
)
}
\arguments{
\item{zip}{ZIP code, a 5-digit numeric string (e.g., \code{"90210"})}

\item{latitude}{Latitude in decimal degrees}

\item{longitude}{Longitude in decimal degrees}

\item{api_key}{AirNow API key}
}
\value{
A one-row tibble with columns \code{reporting_area_code},
\code{reporting_area}, \code{state_code}, \code{latitude}, and \code{longitude}.
}
\description{
\code{get_airnow_reporting_area()} looks up which AirNow reporting area serves
a ZIP code or a latitude/longitude pair. The returned
\code{reporting_area_code} is the \code{area} argument for
\code{\link[=get_airnow_observations]{get_airnow_observations()}}, \code{\link[=get_airnow_forecasts]{get_airnow_forecasts()}}, and
\code{\link[=get_airnow_forecast_history]{get_airnow_forecast_history()}}. Resolution uses AirNow's
current-forecast service, so it requires the area to have a forecast
issued for today; areas without one cannot be resolved this way.
}
\section{Requests made}{

One request per distinct location per session; the result is cached in
memory, so repeated calls for the same location are free. Every request
counts against AirNow's limit of 500 requests per hour per key.
}

\examples{
\dontrun{
get_airnow_reporting_area(zip = "90210")
get_airnow_reporting_area(latitude = 38.3191, longitude = -122.2998)
}
}
