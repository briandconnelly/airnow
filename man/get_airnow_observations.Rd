% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_airnow_observations.R
\name{get_airnow_observations}
\alias{get_airnow_observations}
\title{Get current air quality observations}
\usage{
get_airnow_observations(
  zip = NULL,
  latitude = NULL,
  longitude = NULL,
  area = NULL,
  clean_names = TRUE,
  api_key = get_airnow_key()
)
}
\arguments{
\item{zip}{ZIP code, a 5-digit numeric string (e.g., \code{"90210"})}

\item{latitude}{Latitude in decimal degrees}

\item{longitude}{Longitude in decimal degrees}

\item{area}{Reporting area code such as \code{"ca064"}. See \link{airnow_areas} or
\code{\link[=get_airnow_reporting_area]{get_airnow_reporting_area()}}. When given, \code{zip}, \code{latitude}, and
\code{longitude} are ignored.}

\item{clean_names}{Whether column names should be converted to snake_case
(default: \code{TRUE}). With \code{FALSE}, the API's lowerCamelCase names are kept.}

\item{api_key}{AirNow API key}
}
\value{
A tibble with one row per pollutant. \code{parameter} is a factor with
levels \code{ozone}, \code{pm2.5}, \code{pm10}, \code{co}, \code{no2}, \code{so2}; \code{category_name} is
an ordered factor; \code{aqi} is the NowCast AQI.
}
\description{
\code{get_airnow_observations()} retrieves the most recent hourly readings for
a location. Locate it by ZIP code or latitude/longitude, or give a
reporting area code. The two forms use different AirNow services with
different methodologies:
}
\details{
\itemize{
\item By ZIP code or coordinates, AirNow returns the closest reading for each
pollutant within the reporting area's search radius, and names the site
it came from (\code{site_id}, \code{site_name}).
\item By \code{area}, AirNow returns the area's official value for each pollutant
and omits the site columns, which are \code{NA}.
}

The \code{source} column records which service produced each row.
}
\section{Requests made}{

One request per call, plus one extra request (cached for the session)
when a ZIP code's reporting area shares its name with an area in another
state. There are 26 such names; see \link{airnow_areas}. All requests count
against AirNow's 500-per-hour limit.
}

\section{Time columns}{

\code{hour_observed} uses AirNow's convention of labelling an hour by its
\strong{end}: \code{18} means the period 17:00-17:59. \code{date_observed} and
\code{hour_observed} are local to each reporting area, and \code{local_time_zone}
is an abbreviation R cannot interpret. \code{utc_datetime} is derived from the
reporting area's metadata in \link{airnow_areas} and is the column to use when
comparing areas or joining with \code{\link[=get_airnow_monitors]{get_airnow_monitors()}}. It is \code{NA} when
the area or its time zone cannot be matched.
}

\examples{
\dontrun{
get_airnow_observations(zip = "90210")
get_airnow_observations(latitude = 38.3191, longitude = -122.2998)
get_airnow_observations(area = "ca064")
}
}
